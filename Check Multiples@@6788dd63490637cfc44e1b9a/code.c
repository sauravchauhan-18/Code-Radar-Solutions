#include <stdio.h>

int main() {
    int a,b;
    scanf("%d %d", &a, &b);

    if (a % b == 0) {
        printf("Yes\n", num1, num2);
    } else {
        printf("No\n", num1, num2);
    }

    return 0;
}
